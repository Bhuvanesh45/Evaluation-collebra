# Evaluation-collebra
First open the Jupyter Notebook. And Write the code.
Open the webpage. search the MAc Address. Click on the button signup.
And then automatically generated the API key. 
Then gpoes to Jupyter, import json and requests. write the url variable inside the api key. The file format is list.
Change the list to json . The loads function used to change it the file.
Next,use the index method print company name.
